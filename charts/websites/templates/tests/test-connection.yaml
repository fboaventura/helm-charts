apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "websites.fullname" . }}-test-connection"
  labels:
    {{- include "websites.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "websites.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
